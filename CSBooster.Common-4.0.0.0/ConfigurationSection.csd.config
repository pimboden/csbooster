<?xml version="1.0"?>
<!--
<auto-generated>
    This code was generated by a tool.

     Changes to this file may cause incorrect behavior and will be lost if
    the code is regenerated.
</auto-generated>
-->
<configuration>
	<configSections>
		<section name="customizationSection" type="_4screen.CSB.Common.CustomizationSection"/>
		<section name="widgetSection" type="_4screen.CSB.Common.WidgetSection"/>
		<section name="securitySection" type="_4screen.CSB.Common.SecuritySection"/>
		<section name="siteObjectSection" type="_4screen.CSB.Common.SiteObjectSection"/>
		<section name="siteLinkSection" type="_4screen.CSB.Common.SiteLinkSection"/>
		<section name="wizardSection" type="_4screen.CSB.Common.WizardSection"/>
		<section name="shopSection" type="_4screen.CSB.Common.ShopSection"/>
		<section name="outputTemplatesSection" type="_4screen.CSB.Common.OutputTemplatesSection"/>
		<section name="mapSection" type="_4screen.CSB.Common.MapSection"/>
	</configSections>
	<customizationSection xmlns="http://www.sieme.net/config">
		<!--
		This is just a minimal sample configuration file that shows how to declare
		the configuration sections.

		Because an XML Schema Definition (XSD) is generated for each configuration
		section, it should be trivial to edit these files because you have
		IntelliSense on the XML definition.
		-->
	</customizationSection>
	<widgetSection xmlns="http://www.sieme.net/config">
		<!--
		This is just a minimal sample configuration file that shows how to declare
		the configuration sections.

		Because an XML Schema Definition (XSD) is generated for each configuration
		section, it should be trivial to edit these files because you have
		IntelliSense on the XML definition.
		-->
	</widgetSection>
	<securitySection xmlns="http://www.sieme.net/config">
		<!--
		This is just a minimal sample configuration file that shows how to declare
		the configuration sections.

		Because an XML Schema Definition (XSD) is generated for each configuration
		section, it should be trivial to edit these files because you have
		IntelliSense on the XML definition.
		-->
	</securitySection>
	<siteObjectSection xmlns="http://www.sieme.net/config">
		<!--
		This is just a minimal sample configuration file that shows how to declare
		the configuration sections.

		Because an XML Schema Definition (XSD) is generated for each configuration
		section, it should be trivial to edit these files because you have
		IntelliSense on the XML definition.
		-->
	</siteObjectSection>
	<siteLinkSection xmlns="http://www.sieme.net/config">
		<!--
		This is just a minimal sample configuration file that shows how to declare
		the configuration sections.

		Because an XML Schema Definition (XSD) is generated for each configuration
		section, it should be trivial to edit these files because you have
		IntelliSense on the XML definition.
		-->
	</siteLinkSection>
	<wizardSection xmlns="http://www.sieme.net/config">
		<!--
		This is just a minimal sample configuration file that shows how to declare
		the configuration sections.

		Because an XML Schema Definition (XSD) is generated for each configuration
		section, it should be trivial to edit these files because you have
		IntelliSense on the XML definition.
		-->
	</wizardSection>
	<shopSection xmlns="http://www.sieme.net/config">
		<!--
		This is just a minimal sample configuration file that shows how to declare
		the configuration sections.

		Because an XML Schema Definition (XSD) is generated for each configuration
		section, it should be trivial to edit these files because you have
		IntelliSense on the XML definition.
		-->
	</shopSection>
	<outputTemplatesSection xmlns="http://www.sieme.net/config">
		<!--
		This is just a minimal sample configuration file that shows how to declare
		the configuration sections.

		Because an XML Schema Definition (XSD) is generated for each configuration
		section, it should be trivial to edit these files because you have
		IntelliSense on the XML definition.
		-->
	</outputTemplatesSection>
	<mapSection xmlns="http://www.sieme.net/config">
		<!--
		This is just a minimal sample configuration file that shows how to declare
		the configuration sections.

		Because an XML Schema Definition (XSD) is generated for each configuration
		section, it should be trivial to edit these files because you have
		IntelliSense on the XML definition.
		-->
	</mapSection>
</configuration>
